#include "mass.h"
/**
 * print_most_numbers - Check code
 */
void print_most_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		if !(i == 2 || 1 == 4)
		{
			_putchar(i);
		}
	}
	_putchar('\n');
}
