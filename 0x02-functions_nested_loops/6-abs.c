#include "main.h"
/**
 * _abs - prints out the absolute value of an int
 * @n: The integer to return absolute value of
 *
 * Return: The absolute value of an int
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
