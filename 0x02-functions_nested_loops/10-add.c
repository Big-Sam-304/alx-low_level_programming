#include "main.h"
/**
 * add - Adds two int and returns result
 * @i: one of the int to be summed
 * @j: the second int to be summed
 * Return: the sum of two int
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
